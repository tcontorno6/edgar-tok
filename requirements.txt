# edgar-tok Phase 1 (corpus building)
# Python >= 3.11 (project developed against 3.11, same as ma-target-screener)

# --- cleaning ---
selectolax==0.4.11        # fast HTML parser (lexbor engine); pinned to the version the cleaner was tested with

# --- fetching (Task 4; same battle-tested stack as the ma-target-screener EDGAR client) ---
httpx>=0.27
tenacity>=8.3
pydantic>=2.7

# --- Phase 2: tokenizer training + benchmark (no PyTorch needed) ---
tokenizers>=0.22
transformers>=4.44     # baseline tokenizers only (gpt2 / Qwen / Llama 3)
